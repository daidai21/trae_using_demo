{
  "name": "ecommerce-monorepo",
  "private": true,
  "version": "1.0.0",
  "description": "E-commerce platform Monorepo",
  "scripts": {
    "dev": "pnpm -F web dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18",
    "pnpm": ">=8"
  }
}
